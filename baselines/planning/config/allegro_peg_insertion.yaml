---
T: 10
receding_horizon: False
num_steps: 10
num_trials: 20
visualize: True
experiment_name: 'allegro_peg_insertion'
mode: 'simulation'  # choose from 'simulation', 'hardware', 'hardware_copy'
# fingers: ['index', 'thumb']
fingers: ['index', 'middle', 'thumb']
object_type: 'peg'
sim_device: 'cpu'
include_velocity: True
friction_coefficient: 0.95
kp: 3.0
controllers:
  planning:
    N: 1
    lambda: 1
    variance: 0.01
    step_size: 0.1
    momentum: 0.
    # device: 'cuda:0'
    device: 'cpu'
